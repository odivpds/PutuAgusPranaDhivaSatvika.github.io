const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('console', msg => console.log('PAGE LOG:', msg.text())); page.on('pageerror', err => console.log('PAGE ERROR:', err.message)); await page.goto('http://localhost:5173', {waitUntil: 'networkidle0'}); await page.waitForSelector('#main-content', {timeout: 5000}).catch(() => console.log('Timeout waiting for #main-content')); const styles = await page.eval('*', els => els.filter(el => window.getComputedStyle(el).opacity === '0' || window.getComputedStyle(el).visibility === 'hidden' || window.getComputedStyle(el).display === 'none').map(el => el.tagName + (el.id ? '#'+el.id : '') + (el.className ? '.'+el.className.split(' ').join('.') : ''))); console.log('Hidden elements:', styles); await browser.close(); })();
